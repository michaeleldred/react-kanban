import { render, screen } from '@testing-library/react';

describe("Add Card Button", () => {
    it("changes color when hovered", () => {
        
    });
  
    it("Adds a new card when clicked", () => {
  
    });
  })